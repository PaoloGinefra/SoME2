// all possible states
const states = [0, 1, 2, 3]

// all possible actions
const actions = [0, 1]

const orientations = [0, Math.PI / 2, Math.PI, 3 * (Math.PI / 2)]

const automaton = [
  [3, 1],
  [1, 2],
  [2, 3],
  [3, 0],
]
